# r2 修订记录

- tange/lib/README.md 保留库目录；探鸽 .a 仍不进入补丁或交付包。
- demo.c 与开发分支完全一致，保留测试 UUID/key 和 TG_DEBUG_MODE 默认状态。
- 关闭原厂逐帧 F/+ 的改动由可选诊断移入默认 0001 补丁；其他诊断仍可选。
- 全部补丁改为普通 Git diff；客户通过 git apply 修改文件，验证后自行提交。
- 用户已报告 r1 合并运行正常；r2 仅做静态和补丁验证，未重新编译、板测。

# 交付变更记录

## v1.369.6 / 42229 / legacy-alaw 候选整理

- 按 SDK 示例适配、旧录像、优化建议、板级示例、tange、应用接入分别提交。
- tange 单独补丁，仅包含示例及公共头文件；静态库不复制、不入 patch。
- 分离 lwIP errno=12、MP4 慢操作、文件失败等可选诊断；socket 关闭和 fclose 行为修正未随日志一起删除。
- r1 曾修改默认设备身份行为；r2 按要求撤回，恢复开发版测试 UUID/key 原逻辑。
- 可选 AT 格式化命令按真实 void sd_format(void *) 接口调用，返回仅表示异步受理。
- CDK 仅加入 tange 文件树和包含目录，保留原厂个人调试设置，不同步源工程监视表达式等状态。
- 未迁移生成 makefile、构建产物、备份、无关更新包或开发工具配置。
- SDK 公共头字段与 tange 类型/平台宏说明见 HEADER_CHANGES.md。
- 录像保持旧的 MP4 视频 + alaw 音频；未升级为单文件 AAC 录像，也未替换成 v1.369.8 库。
- 状态：补丁应用已检验；实际编译、库来源/ABI 审计和板测待完成，不是正式发布。
